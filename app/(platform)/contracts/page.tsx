import { ProjectModuleLanding } from "@/app/(platform)/components/project-module-landing";

export default function ContractsIndexPage() {
  return (
    <ProjectModuleLanding
      title="Contracts"
      description="Contracts are project-linked. Open a project to generate and manage contracts from accepted quotations."
    />
  );
}

