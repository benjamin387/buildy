import { ProjectModuleLanding } from "@/app/(platform)/components/project-module-landing";

export default function PurchaseOrdersIndexPage() {
  return (
    <ProjectModuleLanding
      title="Purchase Orders"
      description="Purchase orders are project-linked. Open a project to issue POs and track committed costs."
    />
  );
}

